# Emissor de Duplicata

Repositório para um gerador de duplicatas em PDF a partir de um formulário HTML.

Contém uma implementação de exemplo que usa html2canvas + jsPDF para gerar PDF(s) multi-página a partir do conteúdo da página.
